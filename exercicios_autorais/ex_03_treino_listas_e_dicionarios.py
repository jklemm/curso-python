# Inicialize uma lista vazia, em uma variável chamada `lista_de_frutas`
lista_de_frutas = []

# Adicione uma `manga` na lista utilizando o comando `append(..)`
lista_de_frutas.append('manga')

# Adicione uma `banana` na lista utilizando o comando `append(..)`
lista_de_frutas.append('banana')

# Adicione um `morango` na lista utilizando o comando `append(..)`
lista_de_frutas.append('morango')

# Adicione uma `laranja` na lista utilizando o comando `append(..)`
lista_de_frutas.append('laranja')

# Execute a contagem para ver quantas frutas existem na `lista_de_frutas` utilizando o comando `len(..)`
len(lista_de_frutas)

# lista_de_frutas[0] -> 'manga'
# lista_de_frutas[1] -> 'banana'
# lista_de_frutas[2] -> 'morango'
# lista_de_frutas[3] -> 'laranja'

# Altere a fruta `banana` pela fruta `melancia`
lista_de_frutas[1] = 'melancia'

# Altere a fruta `laranja` pela fruta `melão`
lista_de_frutas[3] = 'melão'

# Altere a fruta `morango` pela fruta `maçã`
lista_de_frutas[2] = 'maçã'

# lista_de_frutas[0] -> 'manga'
# lista_de_frutas[1] -> 'melancia'
# lista_de_frutas[2] -> 'maçã'
# lista_de_frutas[3] -> 'melão'

# Imprima a lista de todas as frutas separadas por vírgula, utilizando o comando `''.join(..)`

# Vai imprimir 'manga, melancia, maçã, melão'
', '.join(lista_de_frutas)
 
# Inicialize uma lista com os números -2, 5, 8 e 14, em uma variável chamada `lista_de_numeros`
# Adicione o número `21` na lista utilizando o comando `append(..)`
# Adicione o número `3` na lista utilizando o comando `append(..)`
# Adicione o número `-15` na lista utilizando o comando `append(..)`
# Adicione o número `38` na lista utilizando o comando `append(..)`
# Efetue a soma de todos os números da lista, utilizando o comando `sum(..)`
# Execute a contagem para ver quantos números existem na lista utilizando o comando `len(..)`
# Encontre a média dos números da lista, utilizando o comando `sum(..)` e dividindo pelo `len(..)`
# Encontre o menor e o maior valor da lista, utilizando respectivamente o comando `min(..)` e `max(..)`
# Efetue e informe a soma do menor valor e do maior valor da lista
# Substitua o valor `-15` por `15`
# Substitua o valor que está no índice 2 da lista pelo valor `9`
# Efetue novamente a soma de todos os números da lista e informe

# Inicialize um dicionário vazio, chamado `lista_de_estados`
# Adicione os 3 estados brasileiros do sul (RS, SC, PR), onde a sigla do estado será 
# o índice e o valor será o nome do estado, ex: 'SC': 'Santa Catarina'
# Adicione o estado SP, São Paulo
# Remova o estado PR utilizando o comando `dicionario.pop('sigla')`
# Imprima todos os estados do dicionário, informando a Sigla - Nome do estado
# Altere o nome do estado do índice 'SC' para 'Santa Catarina - o melhor estado!'
